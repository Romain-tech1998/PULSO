// This file is kept only so any existing import path keeps working.
// The single source of truth for category colors is `./index.ts`;
// do not redefine the map here to avoid the two drifting apart.
export { CATEGORY_COLORS } from './index.js';
